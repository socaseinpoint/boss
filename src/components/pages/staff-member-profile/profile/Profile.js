import React from 'react';
import ProfileDescription from '../../../partials/profile/description';
import mockNormalizedProfileDescriptionData from '../../../../utils/mockNormalizedProfileDescriptionData';

const Profile = () => <ProfileDescription description={mockNormalizedProfileDescriptionData} />;

export default Profile;
